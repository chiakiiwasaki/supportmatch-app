<div class='container'>
  <div class='row'>
    <div class='col-md-12 col-lg-12'>
    <div class="text-right"><%= link_to 'New', new_post_path, class: "btn btn-outline-warning" %></div>
    <h4>投稿一覧</h4>
    <table cellpadding="10" class='table table-hover table-inverse'>
      <thead>
        <tr style="background-color: #EEEEEE;">
          <th>投稿日</th>
          <th>投稿者</th>
          <th>タイトル</th>
          <th>内容</th>
        </tr>
      </thead>
      <tbody>
        <% @posts.each do |post| %>
        <tr>
          <td><%= link_to post.created_at.strftime("%Y-%m-%d %H:%M"), post_path(post), class: "btn btn-link request_#{post.id}" %></td>
          <td><%= post.user.name %></td>
          <td><%= truncate(post.title) %></td>
          <td><%= truncate(post.content) %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <div class="pagination"><%= paginate @posts %></div>
    </div>
  </div>
</div>


<style>
  table{
    table-layout: fixed;
    width: 100%;
    margin-top: 20px;
    overflow-wrap : break-word;
  }
  tr{
    /*上下だけ囲む時の記述*/
    border-width: 1px 0;
    border-color: #CCCCCC;
    border-style: solid;
  }
  .pagination{
    justify-content: center;
    padding-top: 10px;
  }
</style>