<%= form_with(model:[post, post_comment], remote: true) do |f| %>
  <%= f.text_area :content, class: 'form-control' %><br>
  <%= f.submit "Submit Comment", class: 'btn btn-outline-warning' %>
<% end %>
